fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pillow==10.4.0
pytesseract==0.3.10
deep-translator==1.11.4
edge-tts==7.2.8

# coqui-tts (XTTS-v2 cloned-voice engine) is NOT listed here — it's a
# multi-GB dependency (torch et al.) only needed when TTS_ENGINE=xtts.
# It's installed conditionally in the Dockerfile via the INSTALL_XTTS build
# arg, so the default build stays fast and light.
